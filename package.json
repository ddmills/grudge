{
  "name": "@otter/app",
  "version": "1.0.0",
  "scripts": {
    "start": "cd server && npm run start",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "dev": "lerna run dev --parallel --stream --color"
  },
  "devDependencies": {
    "lerna": "^3.1.4"
  }
}
