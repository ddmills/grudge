version: '3.1'

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: pg_dev_user
      POSTGRES_PASSWORD: pg_dev_password
      POSTGRES_DB: grudge
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
